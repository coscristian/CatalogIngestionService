namespace CatalogIngestionService
{
    public class WorkerApiClient //: HttpApiClientBase
    {
        /*public WorkerApiClient(HttpClient httpClient, IConfiguration configuration, string serviceName) : base(httpClient, configuration, serviceName)
        {
        }

        public async Task PostMoviesAsync(MovieInternalDto movie)
        {
            try
            {
                // TODO: Hacer el POST a Catalog API                
                // PostMovieResponseDto response = await PostAsync<MovieInternalDto, PostMovieResponseDto>("/movie", movie);
                var response ="success";
                Console.WriteLine("MY RESPONSE: ", response.ToString());                 

            }catch(Exception ex)
            {
                Console.WriteLine("My Error: ", ex.Message);                             
            }            
        }

        public override void SetAuthHeader()
        {
            throw new NotImplementedException();
        }*/
    }
}